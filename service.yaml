apiVersion: v1
kind: Service
metadata:
  name: parking-service
spec:
  type: NodePort
  selector:
    app: parking-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30007